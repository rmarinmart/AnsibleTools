---
- name: Instalar Openjdk 
  hosts: all:!localhost
  become: yes

  tasks:
    - name: Instalar Openjdk 
      ansible.builtin.apt:
        name: 
           - openjdk-11-jdk
           - openjdk-25-jdk
