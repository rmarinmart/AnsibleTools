---
- name: Apt update
  hosts: all:!localhost
  become: yes

  tasks:
    - name: Actualizar caché
      ansible.builtin.apt:
        update_cache: yes
        cache_valid_time: 3600
      changed_when: false